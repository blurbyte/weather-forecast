import styled from 'styled-components';

const List = styled.li`
  display: flex;
  align-items: center;
  height: 4rem;

  &:hover {
    background-color: rgba(34, 34, 34, 0.1);
  }
`;

export default List;
